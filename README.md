# Company-LittleProducts

Bemblero storefront — production build.

This repository contains the built, deployable static site (HTML/CSS/JS output).
It is uploaded directly to Hostinger's `public_html` folder as-is.

Note: this is compiled/minified output, not editable React source.
